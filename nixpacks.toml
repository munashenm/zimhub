# Railway builds on Linux. A package-lock.json generated on Windows omits the
# Linux native bindings for @tailwindcss/oxide / lightningcss (npm bug #4828),
# which makes `npm ci` fail with "Cannot find native binding".
# Force a clean, platform-correct install instead.
[phases.install]
cmds = ["rm -f package-lock.json", "npm install"]
